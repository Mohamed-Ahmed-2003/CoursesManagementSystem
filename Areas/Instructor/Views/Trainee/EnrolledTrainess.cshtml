@model IEnumerable<CoursesManagementSystem.Models.TraineeCourse>

@{
    ViewBag.Title = "Trainees";
}


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="data-tables datatable-primary">
<table   id="dataTable2" class="text-center">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Trainee.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Trainee.Email)
            </th>  
            
            <th>
                @Html.DisplayNameFor(model => model.Course.Name)
            </th>

            <th>
                @Html.DisplayName("Modify")
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Trainee.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Trainee.Email)
                </td> 
                
                <td>
                    @Html.DisplayFor(modelItem => item.Course.Name)
                </td>

                <td>
                    @Html.ActionLink("Profile", "Details","Trainee", new {area = "", id = item.TraineeID},null) |
                    @Html.ActionLink("Unenroll", "UnEnroll","Trainee", new { tID = item.TraineeID ,cID = item.CourseID },null) |
                </td>
            </tr>
        }
    </tbody>

</table>
    </div>


    